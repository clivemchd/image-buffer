{
  "name": "frame-buffer-image",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "deploy": "npm run build && echo 'Built. Commit dist via GH Actions workflow.'"
  },
  "dependencies": {
    "three": "^0.168.0"
  },
  "devDependencies": {
    "vite": "^5.3.0"
  }
}
